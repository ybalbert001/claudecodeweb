apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-code-ui-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: standard  # Uncomment and adjust based on your cluster's storage class

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-code-ui-projects
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  # storageClassName: standard  # Uncomment and adjust based on your cluster's storage class
