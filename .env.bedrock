# Claude Code UI with AWS Bedrock Configuration
# Copy this file to .env and configure your AWS Bedrock settings

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
PORT=3001

# Frontend port (only used in development mode)
VITE_PORT=5173

# Node environment
NODE_ENV=production

# =============================================================================
# AWS BEDROCK CONFIGURATION
# =============================================================================

# Enable AWS Bedrock API
CLAUDE_CODE_USE_BEDROCK=1

# AWS Region (required)
# Common regions: us-east-1, us-west-2, eu-west-1, ap-southeast-1
AWS_REGION=us-east-1

# AWS Credentials (if not using IAM role or AWS CLI configured credentials)
# Leave blank if running in ECS/EKS with IAM role
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Optional: Bedrock base URL (for LLM Gateway)
# ANTHROPIC_BEDROCK_BASE_URL=https://your-llm-gateway.com/bedrock

# Optional: Skip Bedrock authentication (when gateway handles auth)
# CLAUDE_CODE_SKIP_BEDROCK_AUTH=1

# Optional: Corporate proxy
# HTTPS_PROXY=https://proxy.example.com:8080

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Path to the authentication database file
# This is where user credentials, API keys, and tokens are stored
DATABASE_PATH=/app/data/auth.db

# =============================================================================
# CLAUDE CODE CONFIGURATION
# =============================================================================

# Claude Code context window size (maximum tokens per session)
CONTEXT_WINDOW=160000
VITE_CONTEXT_WINDOW=160000

# Platform mode
# VITE_IS_PLATFORM=false

# =============================================================================
# OPTIONAL: CLAUDE CLI PATH
# =============================================================================

# Uncomment if you have a custom claude cli path
# CLAUDE_CLI_PATH=claude
